{
  "name": "11ty-solid-base",
  "version": "0.1.1",
  "description": "A minimal HTML5 template and the esbuild setup to compile your Solidjs app alongside 11ty.",
  "main": "index.html",
  "scripts": {
    "dev:sass": "sass  --no-source-map --watch src/sass:./_tmp",
    "dev:eleventy": "ELEVENTY_ENV=dev npx @11ty/eleventy --serve",
    "build:sass": "sass  --no-source-map src/sass:./_tmp",
    "build:eleventy": "ELEVENTY_ENV=dev npx @11ty/eleventy --pathprefix=11ty-solid-base",
    "minify:eleventy": "ELEVENTY_ENV=prod npx @11ty/eleventy --pathprefix=11ty-solid-base",
    "build:processcss": "lightningcss --browserslist -o ./_tmp/*.css ./_tmp/*.css",
    "minify:processcss": "lightningcss BROWSERSLIST_CONFIG --minify -o ./_tmp/*.css ./_tmp/*.css",
    "start": "npm-run-all --parallel dev:*",
    "build": "NODE_ENV=dev npm-run-all build:sass build:processcss build:eleventy",
    "minify": "NODE_ENV=production npm-run-all build:sass minify:processcss minify:eleventy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/woodcock3/11ty-solid-start.git"
  },
  "author": "woodcock3",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.16",
    "@11ty/is-land": "^2.0.3",
    "@11ty/eleventy-plugin-webc": "^0.6.0",
    "esbuild": "^0.15.12",
    "esbuild-plugin-solid": "^0.4.2",
    "glob-all": "^3.3.1",
    "lightningcss-cli": "^1.16.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.55.0",
    "solid-js": "^1.6.1"
  },
  "browserslist": [
    "> 0.25%",
    "not IE 11"
  ]
}
